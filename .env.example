# === AutoReply Frontend Environment Variables ===

# Backend API base URL
# Dev:  leave empty — Vite proxy handles /api/v1/* → localhost:5000
# Prod: set to your production backend domain (WITHOUT /api/v1 suffix)
VITE_API_URL=https://salez-backend.onrender.com

# Environment flag
VITE_NODE_ENV=development
